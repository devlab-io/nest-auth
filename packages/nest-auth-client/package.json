{
  "name": "@devlab-io/nest-auth-client",
  "version": "0.0.4",
  "description": "Client library for NestJS Authentication Module",
  "author": "DevLab",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "format": "echo 'Formatting handled at root level'",
    "format:check": "echo 'Format check handled at root level'",
    "lint": "echo 'Linting handled at root level'",
    "lint:check": "echo 'Lint check handled at root level'",
    "test": "echo 'No tests for nest-auth-client'",
    "clean": "rm -rf dist || true"
  },
  "dependencies": {
    "@devlab-io/nest-auth-types": "*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20.19.25",
    "typescript": "^5.7.2"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
